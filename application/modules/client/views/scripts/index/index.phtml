<!-- col-app -->
<div class="col-app animated fadeInDown">
  <div class="widget">
    <div class="widget-head">
      <h4 class="heading">Welcome <?php echo strstr($this->userDetails['fullname'],' ', true); ?>!</h4>
    </div>
    <div class="widget-body innerAll inner-2x"> 
      
      <!-- Table -->
      <table class="datatable table table-striped table-responsive swipe-horizontal table-primary">        
        <!-- Table heading -->
        <thead>
          <tr>
            <th>Licence Name</th>
            <th>Licence Type</th>
            <th>Date from to</th>
            <th>Subdomain</th>
            <th>Status</th>
            <th></th>
          </tr>
        </thead>
        <!-- // Table heading END --> 
        
        <!-- Table body -->
        <tbody>
                 <?php 
        if (!empty($this->programList)) {
            foreach ($this->programList as $pl) :
                ?> 
          <!-- Table row -->
          <tr>
            <td><?php echo $pl['name'] ?></td>
            <td><?php echo $pl['licenceType'] ?></td>
            <td><?php echo $pl['date_from'] ?> <strong>to</strong> <?php echo $pl['date_to'] ?></td>
            <td><?php echo $pl['subdomain'] ?></td>
            <td><?php echo $pl['statusName'] ?></td>
            <td>
            <?php if( $pl['status'] == 5 ){ ?>
            <a href="<?php echo $this->baseUrl() ?>/client/program/setup/licence/<?php echo $pl['id_licence'] ?>" class="btn btn-primary"><i class="fa fa-cog"></i> Program Setup</a>
            <?php } else { ?>
            <a href="<?php echo $this->baseUrl() ?>/client/program/management/l/<?php echo $pl['id_licence'] ?>" class="btn btn-primary"><i class="fa fa-cog"></i> Program Administration</a>
            <?php }  ?>
            </td>
          </tr> 
        <?php
            endforeach
            ;
        }
        ?>                 
        </tbody>
        <!-- // Table body END -->        
      </table>
      <!-- // Table END -->       
    </div>
  </div>
</div>
<!-- // END col-app --> 

